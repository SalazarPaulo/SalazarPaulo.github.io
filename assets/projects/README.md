# Project media

Media used by the Projects modal is stored locally in this folder so the portfolio does not depend on remote raw URLs at runtime.

- `images/`: screenshots and stills used by the carousel.
- `videos/`: project demos.
- `posters/`: lightweight poster images for videos.

The source of truth for the mapping is `project-media-manifest.json`.
