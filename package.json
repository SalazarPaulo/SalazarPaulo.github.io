{
  "name": "dev-arcade-portfolio",
  "private": true,
  "version": "1.0.0",
  "description": "Static arcade portfolio prepared for GitHub Pages.",
  "scripts": {
    "format": "prettier --write index.html styles.css script.js",
    "check": "prettier --check index.html styles.css script.js && node --check script.js",
    "build:css": "lightningcss-cli styles.css --minify --output-file styles.min.css",
    "build:js": "esbuild script.js --minify --outfile=script.min.js",
    "build": "npm run check && npm run build:css && npm run build:js"
  },
  "devDependencies": {
    "esbuild": "0.25.0",
    "lightningcss-cli": "1.30.1",
    "prettier": "3.8.4"
  }
}
